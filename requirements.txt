# Python dependencies for Scientific Computation
# Install with: pip install -r requirements.txt
# Or use: pip install streamlit matplotlib numpy (for latest compatible versions)

streamlit>=1.0
numpy>=1.23
matplotlib>=3.8